# Dependencies / tooling artifacts
# NOTE: dev/package-lock.json is intentionally tracked for reproducible CI installs.
node_modules/

# Local HTTPS dev-server cert pair (machine-local, auto-regenerated by dev/server.js)
localhost.key
localhost.crt

# OS / editor noise
.DS_Store
.vercel/

# coding tools files
opencode.json

# NOTE: .gitignore.public documents files that must never reach a public
# release mirror. This .gitignore is the active ignore file Git actually reads;
# keep them consistent.
