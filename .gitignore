# Dependencies / tooling artifacts
# NOTE: dev/package-lock.json is intentionally tracked for reproducible CI installs.
node_modules/

# Local HTTPS dev-server cert pair (machine-local, auto-regenerated by dev/server.js)
localhost.key
localhost.crt

# OS / editor noise
.DS_Store
.vercel/

# coding tools files
opencode.json

# NOTE: localhost.key / localhost.crt above are machine-local secrets —
# keep them ignored here so they can never reach any remote or public mirror.
